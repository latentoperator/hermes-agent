# Code Review: t_fe3f7c62 — Kanban artifact durability guidance/docs

- **Repo:** `/home/hopewell/.hermes/hermes-agent`
- **Branch:** `wt/t_fe3f7c62`
- **Base SHA:** `77a11a236f8dac46a4e100dd13d7d8a28f38a624`
- **Head SHA:** `e0fd77091594a90375fda48558f6d33de60b0629`
- **Kanban task:** `t_fe3f7c62` (assignee: dante)
- **Reviewer:** code-reviewer profile (glm-5.2, zai)
- **Date:** 2026-07-07
- **Scope constraint honored:** no gateway/dispatcher/runtime mutation, no service restart, no hard kernel rejection implemented — all confirmed.

## Verdict: **Approve** (1 low-severity suggestion, otherwise clean)

The change is well-scoped and accurately implements the durable-artifacts guidance from Wren's standard. It correctly distinguishes artifact *delivery* from artifact *durability* across the injected worker prompt and two user-facing docs, adds matching test assertions, and bumps the prompt-size bound without over-raising it. No critical or warning-level findings.

## Evidence checked

- `git diff` base..head — 4 files, +48/-9, all additions are wording/guidance (no logic).
- `agent/prompt_builder.py` KANBAN_GUIDANCE read in full context (lines 195-279). New sentences sit coherently between the existing workspace and deliverables bullets. Confirmed the new phrases (`delivery, not durability`, `scratch-only artifact path`, `verification checklist`, `working desks, not archives`) all appear in the assembled string.
- `KANBAN_GUIDANCE` length = 6326 chars; new bound is `< 7_000`. Bump is proportional to the added text (~825 chars added).
- `tests/tools/test_kanban_tools.py` — added three `in prompt` assertions in `test_kanban_guidance_in_worker_prompt` and raised the size upper bound from 5_500 to 7_000 in `test_kanban_guidance_prompt_size_bounded`. Both assertions and bound verified against the actual string.
- Ran the full kanban-tools suite myself: **97 passed, 0 failed** (`HOME=/home/hopewell scripts/run_tests.sh tests/tools/test_kanban_tools.py`).
- `website/docs/user-guide/features/deliverable-mode.md` and `kanban.md` — read the new paragraphs in surrounding context. Both read coherently and reinforce (not contradict) the existing "scratch is deleted on completion" wording.
- Verified the actual scratch-cleanup behavior against source: `hermes_cli/kanban_db.py::_cleanup_workspace` (line 4647) is called from `complete_task` and `shutil.rmtree`s managed scratch dirs on completion. So the doc's existing "Deleted when the task completes" claim is accurate, and the new durability warning is warranted — scratch files really are wiped on completion.
- `git status --porcelain` clean; HEAD `e0fd770915` matches `origin/wt/t_fe3f7c62` after fetch (per Dante's handoff; re-confirmed clean tree).

## Findings

### Critical
None.

### Warnings
None.

### Suggestions

**S1 (low) — `kanban.md` new paragraph is slightly orphaned from the `scratch` bullet it extends.**
In `website/docs/user-guide/features/kanban.md`, the new 4-line durability paragraph (lines 69-73) sits at the same indentation as the `scratch` / `dir` / `worktree` sub-bullets but reads as a continuation of the `scratch` bullet specifically. Because it's flush-left under "Workspace" rather than indented under `scratch`, a reader scanning the bullet list sees it as a fourth workspace kind rather than guidance about the first. Consider either indenting it under `scratch` or prefixing with "Note:" / "**On durability:**" so the list structure stays unambiguous. This is purely a readability nit — the content is correct.

## Looks good

- Guidance wording is precise and actionable: names the specific artifact classes (reports, manifests, hash inventories, rollback notes, verification checklists) that must be copied out of scratch, and gives concrete durable destinations.
- Correctly avoids implementing hard kernel rejection, per the task constraint; the doc note flags it as a future design consideration instead.
- Test assertions are content-anchored to meaningful phrases (not change-detector snapshots of exact byte counts), consistent with the project's "behavior contracts over snapshots" rubric.
- Prompt-size upper bound raised proportionally (5_500 → 7_000) with actual growth to 6326 — leaves headroom without inviting runaway bloat.
- All four gates observed: no runtime mutation, no service restart, no deploy, no hard rejection.
