# Code Review: Discord shutdown Missing Access handling

- **Task:** t_62fa4ddb (implementation) / t_71277335 (review)
- **Branch:** `dante/t_62fa4ddb-discord-shutdown-access`
- **Diff:** `b73b446ecf1f..df91bfa53e04`
- **Reviewer:** code-reviewer (DeepSeek v4 Pro)
- **Date:** 2026-07-10 04:02 UTC

## Verdict: APPROVE — no blocking findings

## Evidence

| Claim | Verified | Evidence |
|-------|----------|----------|
| Shutdown/home-channel sends marked `non_conversational` | ✅ | `gateway/run.py:5721-5723`, `:5805-5807`; tests `test_discord_shutdown_warning_is_marked_nonconversational`, `test_discord_home_shutdown_warning_is_marked_nonconversational` |
| Only 50001 Missing Access + 10003 Unknown Channel quieted at DEBUG | ✅ | `_discord_unavailable_target_kind` returns `"forbidden"` only for 50001, `"not_found"` only for 10003; quiet path gated on `nonconversational and unavailable_target_kind is not None` |
| Conversational sends remain ERROR-visible | ✅ | `test_conversational_missing_access_send_still_logs_error` — same 50001 raises ERROR when `non_conversational` is unset |
| 50013 Missing Permissions remains ERROR-visible | ✅ | `test_nonconversational_missing_permissions_still_logs_error` — 50013 returns `None` from `_discord_unavailable_target_kind`, falls through to `else: logger.error(...)` |
| Failed `SendResult` + `error_kind` returned in both paths | ✅ | `return SendResult(success=False, error=str(e), error_kind=error_kind)` executes in both quiet and loud branches |
| Tests pass independently | ✅ | 44/44 across `test_discord_send.py` + `test_gateway_shutdown.py`; zero regressions |
| `classify_send_error` fallback correctly placed | ✅ | `error_kind = unavailable_target_kind or classify_send_error(e)` — Discord-specific classification takes precedence, platform-neutral as fallback |

## Exception classification robustness

- `_discord_unavailable_target_kind` handles: `exc.code` (int), `exc.data["code"]` (dict fallback), string matching on `str(exc)` (for RuntimeError test harness and non-discord exceptions). All six edge cases probed manually — no crash, no misclassification.
- Unknown exception types (`asyncio.CancelledError`, etc.) return `None` safely — `getattr` with default + `isinstance(data, dict)` guard.
- 50013 is correctly NOT matched — string `"missing access"` ≠ `"missing permissions"`, code 50013 ≠ 50001/10003.

## Warnings

None.

## Suggestions

None.

## Remaining risks

- **Low:** The `non_conversational` marker is only set by the gateway shutdown path. If other lifecycle code paths (e.g., future health-check pings) send to stale Discord routes, they won't be quieted. This is by design — scope is intentionally narrow — but worth noting that the mechanism generalizes cleanly if needed.

## Artifacts

- Review artifact: `.code-reviews/20260710-040245-t_62fa4ddb.md`
- Ledger entry: (to be recorded)
